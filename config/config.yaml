storage:
  output_dir: "output_results"
  temp_dir: "output_results/temp"
  log_file: "logs/app.log"
  auto_fetch_dir: "test_videos/landscape"

models:
  depth:
    model_name: "depth-anything/Depth-Anything-V2-Base-hf"
  layer_separation:
    chunk_size: 70